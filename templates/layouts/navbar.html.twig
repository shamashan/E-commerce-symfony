<nav class="navbar navbar-expand-lg bg-body-tertiary" data-bs-theme="dark">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('app_home_page') }}">My Shope</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a class="nav-link active" aria-current="page" href="{{ path('app_home_page') }}">Home</a>
				</li>
				{% if categories is defined %}
					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
							Categories
						</a>
						<ul class="dropdown-menu">

							{% for category in categories %}
								<li>
									<a class="dropdown-item categoryTitle" href="{{ path('app_home_category_filter', {'id': category.id}) }}">{{ category.name }}</a>
								</li>
								{% for subCategory in category.subCategories %}
									<li>
										<a class="dropdown-item" href="{{ path('app_home_product_filter', {'id': subCategory.id}) }}">{{ subCategory.name }}</a>
									</li>
								{% endfor %}
								<li>
									<hr class="dropdown-divider"></li>
							{% endfor %}
						</li>
					</li>
				</ul>
			</li>
		{% endif %}
		{% if is_granted("ROLE_ADMIN") %}
			<li class="nav-item dropdown">
				<a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
					Admin
				</a>
				<ul class="dropdown-menu">
					<li>
						<a class="dropdown-item" href="{{ path('app_category') }}">Categories List</a>
					</li>
					<li>

						<a class="dropdown-item" href="{{ path('app_category_add') }}">Add new categories</a>
					</li>
					<li>
						<hr class="dropdown-divider">
					</li>
					<li>
						<a class="dropdown-item" href="{{ path('app_sub_category_index') }}">Sub Categories List</a>

					</li>
					<li>
						<a class="dropdown-item" href="{{ path('app_sub_category_new') }}">Add new sub category</a>

					</li>
					<hr class="dropdown-divider">


					<li>
						<a class="dropdown-item" href="{{ path('app_product_index') }}">Products List</a>

					</li>
					<li>
						<a class="dropdown-item" href="{{ path('app_product_new') }}">Add new product</a>

					</li>

					<li>
						<hr class="dropdown-divider">
					</li>
					<li>
						<a class="dropdown-item" href="{{ path('app_user') }}">Users</a>
					</li>
				</ul>
			</li>
		{% endif %}
		{% if app.user  %}
			<li class="nav-item">
				<a class="nav-link" href="{{path('app_logout')}}">Logout</a>
			</li>
		{% endif %}
	</ul>
	{% if app.user == false %}
		<li class="nav-item">
			<a class="btn btn-primary" href="{{path('app_login')}}">
				Log in</a>
		</li>
		<li class="nav-item">
			<a class="btn btn-outline-light" href="{{path('app_register')}}">
				Register</a>
		</li>
	{% endif %}
</div>
<div>
	<form class="d-flex"><input class="form-control me-2 my-0" type="search" placeholder="Search" aria-label="Search"><button class="btn btn-outline-light" type="submit">Search</button>
	</form>
</div></div></nav>
